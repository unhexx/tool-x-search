# Задачи локальным агентам: tool-x-search

- [ ] T-001 Схема `/schema` с отдельными именами-заглушками под каждый variant (сейчас одно имя `x_search`).
- [ ] T-002 Фикстуры на `x_user_search` / `x_thread_fetch` с разными полями.
- [ ] T-003 Тест: без `TOOL_X_FIXTURES` ни один код-путь не читает сеть.
- [ ] T-004 Короткий блок для SYSTEM_PROMPT потребителя: «X только server-side».
- [ ] T-005 Не добавлять Nitter/мосты без отдельного решения владельца репо.

## Закрыто

- [x] T-000 Отказ по умолчанию + тест.

